/* declare module "UI5bH" {
    class Element extends Phaser.GameObjects.GameObjectFactory {
        constructor(config: any){super(config)}
        id: string
        // delete(): void {
            // this.delete();
        // }
    }

    class Button extends Element {
        constructor(config: Phaser.GameObjects.Text) {
            super(config)
            this.text(config.x,config.y,config.text,config.style).setInteractive();
        }
    }

    class Text extends Element {
        constructor(config: Phaser.GameObjects.Text) {
            super(config)
            this.text(config.x,config.y,config.text,config.style);
        }
    }

    // class Image extends Element {
    //     constructor(config: Phaser.GameObjects.Image) {
    //         super(config)
    //         this.image(config.x,config.y,config.texture)
    //     }
    // }
}

function makeUI() {

} */
